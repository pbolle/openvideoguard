# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                               controllers.EventController.index(page:Int=1)
GET     /events/list/:page              controllers.EventController.index(page:Int)
GET     /reindex                        controllers.ReindexController.index()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file                   controllers.Assets.versioned(path="/public", file: Asset)

GET     /resources/*file                controllers.Resource.at(path="/resources", file)
